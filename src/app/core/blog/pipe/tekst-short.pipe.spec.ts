import { TekstShortPipe } from './tekst-short.pipe';

describe('TekstShortPipe', () => {
  it('create an instance', () => {
    const pipe = new TekstShortPipe();
    expect(pipe).toBeTruthy();
  });
});
